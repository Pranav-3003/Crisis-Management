[project]
name = "crisis-management-openenv"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:main"